class Solution {
    public int maximumWealth(int[][] accounts) {
        int max=0;
        int ans =0;
        int sum=0;
        for(int i=0;i<accounts.length;i++){
            sum=0;
            for(int j=0;j<accounts[0].length;j++){
                
                sum+=accounts[i][j];
            }
            ans=Math.max(sum,max);
            max=ans;
             
        }
        return max;
    }
}
